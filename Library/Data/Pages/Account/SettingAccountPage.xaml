<Page x:Class="Library.Data.Pages.Account.SettingAccountPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Library.Data.Pages.Account"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d"
      Title="SettingAccountPage" Height="470" Width="880">
    <Border Height="470"
            Width="880"
            CornerRadius="10">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Button Grid.Row="0"
                    FontSize="30"
                    Width="400"
                    Height="100"
                    Content="Change Name"
                    Style="{DynamicResource ButtonStyle}"
                    materialDesign:ButtonAssist.CornerRadius="20"
                    Click="ChangeName_OnClick" />
            <Button Grid.Row="1"
                    FontSize="30"
                    Width="400"
                    Height="100"
                    Content="Change Surname"
                    Style="{DynamicResource ButtonStyle}"
                    materialDesign:ButtonAssist.CornerRadius="20"
                    Click="ChangeSurname_OnClick" />
            <Button Grid.Row="2"
                    FontSize="30"
                    Width="400"
                    Height="100"
                    Content="Change Login"
                    Style="{DynamicResource ButtonStyle}"
                    materialDesign:ButtonAssist.CornerRadius="20"
                    Click="ChangeLogin_OnClick" />
            <Button Grid.Row="3"
                    FontSize="30"
                    Width="400"
                    Height="100"
                    Content="Change Password"
                    Style="{DynamicResource ButtonStyle}"
                    materialDesign:ButtonAssist.CornerRadius="20"
                    Click="ChangePassword_OnClick" />
        </Grid>
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" Color="WhiteSmoke" />
        </Border.Effect>
    </Border>
</Page>